//! `sdr-acars-cli` entry point. Implementation lands in Task 9
//! of the ACARS DSP crate plan
//! (`docs/superpowers/plans/2026-04-28-acars-dsp-crate.md`).
//! Scaffold only — see Task 1.

fn main() {
    // Stub — full CLI lands in Task 9. Exits cleanly so `cargo
    // build -p sdr-acars` succeeds while later tasks are still
    // pending.
}
